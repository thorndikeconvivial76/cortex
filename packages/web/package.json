{
  "name": "@cortex/web",
  "version": "1.0.0",
  "private": true,
  "description": "cortex.sh — landing page, installer, subscriber verification",
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --format esm --clean",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "fastify": "^5.8.2",
    "@fastify/cors": "^11.2.0",
    "@fastify/static": "^8.0.0",
    "@fastify/rate-limit": "^10.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0"
  }
}
